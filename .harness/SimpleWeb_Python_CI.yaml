pipeline:
  name: SimpleWeb_Python_CI
  identifier: SimpleWeb_Python_CI
  projectIdentifier: DemoTx
  orgIdentifier: default
  tags:
    ai_generated: "true"
  properties:
    ci:
      codebase:
        connectorRef: account.Github_OAuth_1702454216196
        repoName: simple-web-backend
        build: <+input>
  stages:
    - stage:
        name: Build_and_artifact
        identifier: Build_and_artifact
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          caching:
            enabled: true
            override: false
            paths: []
          buildIntelligence:
            enabled: false
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Docker
            spec: {}
          execution:
            steps:
              - step:
                  type: Run
                  name: test connection
                  identifier: test_connection
                  spec:
                    shell: Sh
                    command: echo "pipeline started"
              - step:
                  type: Semgrep
                  name: Semgrep
                  identifier: Semgrep
                  spec:
                    mode: orchestration
                    config: default
                    target:
                      type: repository
                      detection: auto
                    advanced:
                      log:
                        level: info
                      fail_on_severity: critical
                      args:
                        cli: "--json --output semgrep-results.json --disable-version-check"
                    settings:
                      SEMGREP_APP_TOKEN: <+secrets.getValue("project.SEMGREP_APP_TOKEN")>
                    auth:
                      access_token: <+secrets.getValue("project.SEMGREP_APP_TOKEN")>
                  timeout: 10m
            rollbackSteps: []
  description: This pipeline was updated by Harness AI on 2025-06-26 06:59:11 UTC by Gurpreet.
